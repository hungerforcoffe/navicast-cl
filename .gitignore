# Entornos
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Datos locales (staging de Bronze). Lo pesado vive en S3, no en git.
data/
*.zip
*.csv
app/*.html   # mapas generados (datos embebidos); regenerar con: python -m navicast.viz

# Secretos / credenciales (NUNCA versionar)
.env
.aws/
*.pem

# SO
.DS_Store
Thumbs.db
